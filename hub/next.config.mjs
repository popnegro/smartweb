/** @type {import('next').NextConfig} */
const nextConfig = {
  // Para desplegar el sitio en una subcarpeta, cambia este valor.
  // Por ejemplo, si usas '/hub', tu sitio será accesible en `dominio.com/hub`.
  // Déjalo como un string vacío ('') si el sitio va en la raíz del dominio.
  basePath: '/hub',
};

export default nextConfig;